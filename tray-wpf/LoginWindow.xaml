<Window x:Class="DeepseekPlanbarTray.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Width="520" Height="760" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        WindowStartupLocation="CenterScreen">
    <!-- Margin 28 is shadow fade room，同主窗/设置窗 -->
    <Border CornerRadius="14" Background="{DynamicResource WindowBgBrush}" Margin="28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="2" Opacity="0.25" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏（可拖动） -->
            <DockPanel Grid.Row="0" Margin="2,0,2,8" MouseLeftButtonDown="TitleBarDrag">
                <Image Source="pack://application:,,,/deepseek-logo.png" Width="18" Height="18"
                       DockPanel.Dock="Left"/>
                <TextBlock Text="登录 DeepSeek 同步用量 Token" FontSize="15" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button Content="✕" Style="{StaticResource ChromeCloseButton}"
                        DockPanel.Dock="Right" Click="CloseClick"/>
            </DockPanel>

            <TextBlock Grid.Row="1" x:Name="StatusText" Text="正在加载登录页..."
                       FontSize="11" TextWrapping="Wrap" Margin="2,0,2,8"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>

            <Border Grid.Row="2" CornerRadius="10" ClipToBounds="True"
                    Background="{DynamicResource CardBgBrush}">
                <wv2:WebView2 x:Name="WebView"/>
            </Border>
        </Grid>
    </Border>
</Window>
